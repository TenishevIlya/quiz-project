` npm init`
`npm install lite-server --save-dev`
В ридми обе эти команды не нужны, ты это как разработчик делаешь.
Всё что надо делать разрабу для твоего проекта это инструкции
npm install и потом npm start или npm run build

---------------------------

добавь команду запуска линтера по своим файлам в package.json -> scripts

---------------------------

на выходе после npm run build:js должен быть es5, а у тебя es6, стало быть babel делает не всё.
проверить легко, у тебя вместо let должно быть var, пока в ие11 не запустится, то что-то не так

---------------------------

в консоли браузера вижу
Refused to apply style from 'http://localhost:63342/quiz-project/public/node_modules/normalize.css'
because its MIME type ('text/html') is not a supported stylesheet MIME type, and strict MIME checking is enabled.

стало быть некорректно подключил normalize. Лучше уж тогда через cdn подключи пока что.
Потому что с node_modules например у тебя его нет в package.json, соответственно у меня он не установился +
с node_modules можно будет его взять если использовать webpack, я через занятие расскажу об этом

---------------------------

у тебя есть кнопка назад которая меня толкает вперед по квизу и засчитывает мой ответ,
который я не хотел давать, не надо так))
лучше уж совсем удалить кнопку

---------------------------

твоя радиокнопка выглядит как чебокс, с галкой внутри и хочется
выбрать несколько вариантов ответа, это плохо для UX, используй точку в кружке вместо чекбокса

---------------------------

давай всё ж заменим input type=text на что-то удобнее, чтобы не было ошибок ввода текста.
Опять же по UX с этим проблема

---------------------------

строки текста типа "Мои тесты" надо удалить из паблика,
должно всё ж из json схватываться, как мы с тобой обсуждали

---------------------------

попробуй добавить ещё select

